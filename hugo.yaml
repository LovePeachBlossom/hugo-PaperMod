baseURL: "https://wf0904.cn/"
languageCode: "zh-cn"
title: "我的个人博客"

# 启用 robots.txt 生成，对 SEO 友好
enableRobotsTXT: true

# 生产环境构建设置
buildDrafts: false
buildFuture: false
buildExpired: false

# 分页设置：每页 10 篇文章
pagination:
  pagerSize: 10

# -------------------------------------------------------------------------
# 新增核心配置：Markdown 渲染与代码高亮
# -------------------------------------------------------------------------
markup:
  goldmark:
    renderer:
      unsafe: true # 【关键】允许在 Markdown 中使用 HTML，这是 Bilibili 和剧透组件必须的！
  highlight:
    noClasses: false
    # 使用 Dracula 配色，完美契合怪奇物语的深色氛围
    style: dracula
    # 显示行号，增加技术博客的专业感
    lineNos: true
    # 允许代码块内联样式
    guessSyntax: true

# 输出格式设置，保留 JSON 以支持搜索功能
outputs:
  home:
    - HTML
    - RSS
    - JSON

# -------------------------------------------------------------------------
# 主题参数设置 (PaperMod)
# -------------------------------------------------------------------------
params:
  env: production
  defaultTheme: auto # 自动跟随系统，但我们会用 CSS 强制优化深色模式体验
  
  # 顶部左侧的站点名称与图标
  label:
    text: "My Blog"
    # icon: /apple-touch-icon.png  # 取消注释并添加图标文件到 static/ 目录
    iconHeight: 35

  # 首页个人信息卡片
  profileMode:
    enabled: true
    title: "你好，欢迎来到我的博客"
    subtitle: "热爱技术，热爱生活"
    # imageUrl: "https://github.com/LovePeachBlossom.png" # 建议取消注释并确头像可用
    imageWidth: 120
    imageHeight: 120
    buttons:
      - name: ⏱️ 归档
        url: /archives
      - name: 🔍 搜索
        url: /search

  # 社交链接
  socialIcons:
    - name: github
      url: "https://github.com/LovePeachBlossom"
    - name: bilibili
      url: "https://space.bilibili.com/407681508"
    - name: email
      url: "mailto:932505460@qq.com"

  # 【新增】SEO 与分享优化
  # 当文章有封面图时，分享到社交媒体会显示大图
  schema:
    publisherType: Person
    name: Me # 替换为您的名字
  
  # images: ["apple-touch-icon.png"] # 默认分享图（取消注释并添加图标文件后启用）

  # 【新增】密码保护功能说明
  # 1. 为文章添加密码保护：在文章的 front matter 中添加：
  #    passwordHash: "哈希值"  # 使用 /password-generator.html 工具生成
  #    passwordHint: "密码提示"  # 可选
  # 2. 访问 /password-generator.html 生成密码哈希值
  # 3. 密码验证成功后会在浏览器中记住解锁状态

# -------------------------------------------------------------------------
# 顶部导航栏
# -------------------------------------------------------------------------
menu:
  main:
    - identifier: search
      name: 🔍 搜索
      url: /search/
      weight: 10
    - identifier: archives
      name: ⏱️ 归档
      url: /archives/
      weight: 20
    - identifier: protected
      name: 🔒 密码保护
      url: /protected/
      weight: 30